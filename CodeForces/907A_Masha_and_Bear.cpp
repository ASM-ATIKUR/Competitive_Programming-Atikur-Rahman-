#include <bits/stdc++.h>
using namespace std;

int main(void)
{
    int v1, v2, v3, vm, x, y;
    scanf("%d %d %d %d", &v1, &v2, &v3, &vm);

    x=2*v3;
    if(x>=v2) x=v2-1;

    if(x>=vm && 2*vm>x ) printf("%d")

}
